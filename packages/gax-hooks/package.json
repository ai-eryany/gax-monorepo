{
  "name": "@gax/hooks",
  "private": true,
  "version": "1.0.0",
  "description": "Core Hooks for the project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "format": "prettier src --write",
    "clean": "rm -rf dist",
    "reset": "rm -rf node_modules && rm -rf package-lock.json"
  },
  "dependencies": {
    "@gax/utils": "^1.0.0",
    "react": "^18.3.1",
    "react-router-dom": "^6.25.1"
  },
  "devDependencies": {
    "@gax/types": "^1.0.0",
    "@types/react": "^18.3.3",
    "typescript": "^5.5.4"
  }
}
