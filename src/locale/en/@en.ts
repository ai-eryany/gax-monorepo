import { Language } from '@interfaces/core';
import statements from './statements';
import actions from './actions';
import titles from './titles';
import messages from './messages';

const en: Language = {
  lang: 'en',
  statements,
  actions,
  titles,
  messages,
};

export default en;
