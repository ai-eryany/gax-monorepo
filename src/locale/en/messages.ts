import { Error, Info, Messages, Success } from '@interfaces/core';

const success: Success = undefined;

const info: Info = undefined;

const error: Error = undefined;

const messages: Messages = {
  success,
  info,
  error,
};

export default messages;
